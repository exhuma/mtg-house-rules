<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or "MTG House Rules" }}</title>
  <style>
    body { font-family: sans-serif; margin: 0; padding: 0; }
    .navbar {
      display: flex;
      background: #222;
      padding: 0.5em 1em;
    }
    .navbar a {
      color: #fff;
      text-decoration: none;
      padding: 0.5em 1em;
      margin-right: 0.5em;
      border-radius: 4px 4px 0 0;
      background: #333;
      transition: background 0.2s;
    }
    .navbar a.active, .navbar a:hover {
      background: #0077cc;
    }
    .container {
      max-width: 800px;
      margin: 2em auto;
      background: #fff;
      padding: 2em;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.07);
    }
  </style>
</head>
<body>
  <nav class="navbar">
    {% for page in collections.pages %}
      <a href="{{ page.url }}" class="{% if page.url == page.url or page.url == page.inputPath %}active{% endif %}">{{ page.data.title or page.fileSlug }}</a>
    {% endfor %}
  </nav>
  <div class="container">
    {{ content | safe }}
  </div>
</body>
</html>
